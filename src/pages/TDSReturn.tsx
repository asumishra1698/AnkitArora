import SimplePage from "./SimplePage";

function TDSReturn() {
  return (
    <SimplePage
      title="TDS Return"
      description="TDS return filing support with reconciliation, challans, and compliance tracking."
    />
  );
}

export default TDSReturn;
