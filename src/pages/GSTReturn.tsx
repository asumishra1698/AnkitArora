import SimplePage from "./SimplePage";

function GSTReturn() {
  return (
    <SimplePage
      title="GST Return"
      description="GST return preparation and filing with reconciliation support and deadline tracking."
    />
  );
}

export default GSTReturn;
